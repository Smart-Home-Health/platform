[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
markers =
    integration: tests that exercise external-ish surfaces (mocked)
    slow: slower tests (large data / round-trips)
    acme_integration: real ACME round-trip against pebble (needs the pebble stack; auto-skips otherwise)
